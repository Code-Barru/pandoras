{
  "name": "pandoras-server",
  "version": "1.0.0",
  "description": "",
  "main": "bot.js",
  "scripts": {
    "clean": "del /s /q dist",
    "build": "tsc --build",
    "start": "npm run clean && npm run build && node dist/base/index.js",
    "startNoClean": "npm run build && node dist/base/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "discord.js": "^14.15.3",
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "prisma": "^5.17.0"
  }
}
